# test file for virtualenv
---
- hosts: localhost
  connection: local
  become: true
  pre_tasks:
    - name: include tasks
      include: "{{ playbook_dir }}/tasks/pre.yml"
  roles:
    - ../../
#  vars:
#    virtualenv_python_version: 3
